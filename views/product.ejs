<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
    <link rel="stylesheet" href="/views/product.css">
</head>
<body>
    <div class="container">
        <h1><%= product.name %></h1>
    </div>
    <div class="image">
        <img src="/uploads/<%= product.image %>" alt="<%= product.name %>" width="300">
    </div>
    <div class="description">
        <h3>Описание:</h3>
        <p><%= product.description %></p>
    </div>    
    <div class="price">
        <div class="pr" onclick="buy()">Цена: $<%= product.price %></div>
        <button id="buyBtn" class="buy_product" data-product-id="<%= product.id %>">Купить</button>

    </div>
    <a href="/">Вернуться в каталог</a>

    <script src="/script.js"></script>
</body>
</html>
