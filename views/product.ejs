<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
    <link rel="stylesheet" href="/views/product.css">
</head>
<body>
    <div class="container">
        <h1><%= product.name %></h1>
    </div>
    <div class="image">
        <img src="/uploads/<%= product.image %>" alt="<%= product.name %>" width="300">
    </div>
    <div class="description">
        <h3>Описание:</h3>
        <p><%= product.description %></p>
    </div>    
    <div class="price">
        <div class="pr">Цена: $<%= product.price %></div>
        <button id="buyBtn" class="buy_product" data-product-id="<%= product.id %>">Купить</button>
    </div>

    <div class="tags">
        <h2>Теги</h2>
        <!-- Здесь мы добавляем теги внутри span -->
        <p id="productTags"></p>
    </div>

    <script>
        // Получаем теги
        let tags = "<%= product.tags %>";
        
        // Убираем символы "×" и разделяем теги
        tags = tags.replace(/×/g, '').trim();
        let tagsArray = tags.split(',');

        // Выводим теги в столбик
        let tagsContainer = document.getElementById('productTags');
        tagsArray.forEach(tag => {
            let tagElement = document.createElement('span');
            tagElement.textContent = tag;
            tagsContainer.appendChild(tagElement);
        });
    </script>

    <a href="/catalog">Вернуться в каталог</a>

    <script src="/script.js"></script>
</body>
</html>
